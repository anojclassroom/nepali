
name: Validate Playlist

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify M3U exists and is non-empty
        run: |
          test -s playlists/nepali.m3u
